pipeline {
   agent any

   stages {
      stage('build') {
         steps {
            sh 'pip install -r requirements.txt'
         }
         }


      stage('runpy') {
        steps{      
          sh 'python3 tests.py'
        }
        }

  }
}
